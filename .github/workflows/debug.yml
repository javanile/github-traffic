name: Traffic Update

on:
  push:
    branches: [ main ]

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - run: curl -s -u ${{ secrets.CURL_USER }} https://api.github.com/repos/javanile/vtiger/traffic/popular/referrers
